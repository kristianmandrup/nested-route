// Generated by LiveScript 1.2.0
(function(){
  var nested, route;
  nested = require('../../index');
  route = nested.route;
  describe('route.toString()', function(){
    afterEach(function(){
      crossroads.resetState();
      return crossroads.removeAllRoutes();
    });
    return specify('should help debugging', function(){
      var count, requests, a;
      count = 0;
      requests = [];
      a = crossroads.addRoute('/{foo}_{bar}');
      a.matched.add(function(foo, bar){
        return expect(null).toEqual('fail: not a trigger test');
      });
      return expect(a.toString()).toBe('[Route pattern:"/{foo}_{bar}", numListeners:1]');
    });
  });
}).call(this);
